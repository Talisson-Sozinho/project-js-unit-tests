{
  "name": "sd-21-block8-project-js-unit-tests",
  "version": "1.0.0",
  "description": "Project for block 8",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "lint": "eslint --no-inline-config --no-error-on-unmatched-pattern -c .eslintrc.json . --ext .js,.jsx"
  },
  "jest": {
    "rootDir": "./tests/"
  },
  "keywords": [
    "javascript",
    "tests",
    "assert",
    "jest"
  ],
  "devDependencies": {
    "eslint-config-trybe-fundamentals": "1.1.1",
    "jest": "24.9.0"
  }
}
